/** Cloudflare Web Analytics token — optional; Pages can auto-inject when enabled in dashboard Metrics tab. */
window.LS_CF_WEB_ANALYTICS_TOKEN = '';

/** Optional GA4 Measurement ID (G-XXXXXXXXXX). Leave empty until you create a GA4 property. */
window.LS_GA_MEASUREMENT_ID = '';